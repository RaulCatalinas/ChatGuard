import 'package:flutter/material.dart';

const String appName = 'ChatGuard';
const Size windowSize = Size(800, 600);
const bool centerWindow = true;
const bool resizeWindow = true;
const bool maximizeWindow = false;
